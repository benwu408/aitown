services:
  backend:
    build: ./backend
    ports:
      - "8000:8000"
    env_file:
      - path: .env
        required: false
    environment:
      - DB_PATH=/app/data/agentica.db
    volumes:
      - ./backend:/app
      - backend-data:/app/data
    stop_grace_period: 10s
    restart: unless-stopped

  frontend:
    build: ./frontend
    ports:
      - "5173:5173"
    volumes:
      - ./frontend:/app
      - /app/node_modules
    depends_on:
      - backend
    environment:
      - VITE_WS_URL=ws://localhost:8000/ws

volumes:
  backend-data:
